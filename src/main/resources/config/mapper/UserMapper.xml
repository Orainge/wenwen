<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orainge.wenwen.mybatis.mapper.UserMapper">

    <!-- 登录时获取的用户信息 -->
    <select id="getUserLoginInfoByPricipal" parameterType="String" resultType="User">
        select username, email, password from user where (username = #{principal} or email = #{principal}) and user_status = 0
    </select>

    <!-- 获取要保存在浏览器的用户信息 -->
    <select id="getUserInfoByPricipal" parameterType="String" resultType="User">
        select * from user where (username = #{principal} or email = #{principal}) and user_status = 0
    </select>
</mapper>